# CodeBuddy v5.0 — All Zero-Cost Dependencies
# Install: pip install -r requirements.txt

flask>=3.0.0
flask-login>=0.6.3
flask-bcrypt>=1.0.1
python-dotenv>=1.0.0
requests>=2.31.0
gtts>=2.5.0

# click>=8.2.1 fixes conflict between gTTS and typer
click>=8.2.1

# Collab real-time (threading mode — eventlet removed, was causing session bugs)
flask-socketio>=5.3.6

# Redis for rate limiting (optional — falls back to in-memory if unavailable)
redis>=5.0.0

# Video Analyzer frame extraction (optional — text-only mode without it)
# pip install opencv-python