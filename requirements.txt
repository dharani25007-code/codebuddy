# CodeBuddy v5.0 — All Zero-Cost Dependencies
# Install: pip install -r requirements.txt

flask>=3.0.0
flask-login>=0.6.3
flask-bcrypt>=1.0.1
python-dotenv>=1.0.0
requests>=2.31.0
gtts==2.3.2
gunicorn>=21.2.0



# Collab real-time (gevent mode — required for WebSocket support on Render)
flask-socketio>=5.3.6
gevent>=24.2.1
gevent-websocket>=0.10.1
packaging>=23.0

# Redis for rate limiting (optional — falls back to in-memory if unavailable)
redis>=5.0.0

# Optional if you later migrate from SQLite to PostgreSQL
psycopg2-binary>=2.9.9

# Video Analyzer frame extraction (optional — text-only mode without it)
# pip install opencv-python